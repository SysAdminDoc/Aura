# Aura local build configuration.
# Copy this file to local.properties and fill only the values you need.

sdk.dir=C:/Users/Xray/AppData/Local/Android/Sdk

# Optional provider keys. The app also lets users configure keys in Settings.
pexels.api.key=
pixabay.api.key=
freesound.api.key=
soundcloud.client.id=
stability.ai.key=

# Required only for signed release builds.
signing.keystore.path=../freevibe.jks
signing.keystore.password=
signing.key.alias=
signing.key.password=
