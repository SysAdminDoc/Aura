# Network Endpoint Inventory

This runbook is the reviewed inventory for app-initiated network surfaces.
`tools/network_endpoint_inventory_check.py` checks this file and
`docs/security/network-endpoints.json` against literal HTTP(S) hosts in the
network-facing Kotlin source roots.

## Guard

Run:

```powershell
py -3 tools\network_endpoint_inventory_check.py --inventory docs\security\network-endpoints.json --repo-root .
```

The guard scans:

- `app/src/main/java/com/freevibe/di`
- `app/src/main/java/com/freevibe/data/remote`
- `app/src/main/java/com/freevibe/data/repository`
- `app/src/main/java/com/freevibe/service`

Every literal HTTP(S) host in those roots must appear in
`docs/security/network-endpoints.json`. The Markdown runbook must mention every
endpoint ID and reviewed host.

## Endpoints

| ID | Hosts | Auth location | Data sent | Cache / rate / fallback / owner |
| --- | --- | --- | --- | --- |
| `wallhaven-api` | `wallhaven.cc` | Optional query parameter `apikey`. | Search query, sorting, category, purity, resolution, ratio, color, and page. | Wallpaper metadata/image URLs can be cached. Cache fallback is available. Kill switch: `wallhaven_provider_enabled`. Owner: maintainer. |
| `bing-daily` | `www.bing.com`, `www2.bing.com`, `cn.bing.com` | None. | Market, index, count, response format. | Wallpaper metadata/image URLs can be cached. Retry uses reviewed Bing fallback hosts. Kill switch: `bing_provider_enabled`. Owner: maintainer. |
| `pexels-api` | `api.pexels.com` | `Authorization` header. | Search query, orientation, size, page, per-page. | Provider media URLs can be cached. Pexels is Discover enhancement-only when no base inventory exists. Kill switch: `pexels_provider_enabled`. Owner: maintainer. |
| `pixabay-api` | `pixabay.com`, `i.vimeocdn.com` | Query parameter `key`. | Search query, media type, orientation, safe-search, order, category, page. | 24-hour request cache, 429 backoff from provider headers, stale-cache fallback. Kill switch: `pixabay_provider_enabled`. Owner: maintainer. |
| `reddit-json` | `www.reddit.com`, `i.redd.it`, `i.imgur.com` | None. | Subreddit, search query, sort, limit, cursor, time range, raw JSON flag. | Returned image URLs can be cached. No host-specific backoff yet. Kill switch: `reddit_provider_enabled`. Owner: maintainer. |
| `openverse-audio` | `api.openverse.org` | None. | Search query, page, page size, mature flag. | Returned preview URLs can be cached by favorite/offline paths. Source metrics record failures. Owner: maintainer. |
| `freesound-v2` | `freesound.org` | Query parameter `token`. | Search query, fields, page, page size, filter, sort. | Freesound 429 retry is scoped by `RateLimitInterceptor`; returned previews can be cached. Owner: maintainer. |
| `soundcloud-api` | `api-v2.soundcloud.com`, `soundcloud.com` | Query parameter `client_id`. | Search query, license filter, duration bounds, limit, offset. | Stream URLs can be cached. Blank client ID returns no results. Owner: maintainer. |
| `audius-api` | `api.audius.co`, `audius.co` | None. | Search query and limit. | Returned stream URLs can be cached. Source metrics record failures. Owner: maintainer. |
| `ccmixter-api` | `ccmixter.org` | None. | Search term, limit, sort, response format. | Returned download URLs can be cached. Existing HTTP fallback and cleartext exception remain tracked as separate release-gate work. Owner: maintainer. |
| `open-meteo-api` | `api.open-meteo.com` | None. | Latitude, longitude, current weather flag. | No media cache. Weather effects degrade when current weather is unavailable. Kill switch: `weather_effects_enabled`. Owner: maintainer. |
| `stability-api` | `api.stability.ai` | Bearer `Authorization` header. | Prompt, aspect ratio, output format, optional style preset, optional negative prompt. | Generated bytes are stored in app-private files and pruned to 50 recent images. Kill switch: `generated_content_provider_enabled`. Owner: maintainer. |
| `youtube-newpipe` | `www.youtube.com` | None. | Video ID in watch URL. | Resolved stream URLs have a short in-memory TTL and can be cached by user action. Kill switch: `youtube_provider_enabled`. Owner: maintainer. |
| `aura-collection-links` | `aura.app` | None. | No app-initiated network; links are parsed as import locators. | Invalid links are rejected by import validation. Owner: maintainer. |
| `firebase-community` | `firebaseio.com`, `firebasestorage.googleapis.com`, `identitytoolkit.googleapis.com`, `firebaseappcheck.googleapis.com`, `firebaseinstallations.googleapis.com`, `cloudfunctions.net` | Firebase SDK credentials and App Check tokens. | Firebase UID, community upload metadata, reports, votes, follows, blocks, profile edits, storage uploads, callable payloads. | Callable quota/dedupe ledgers enforce write limits. Community paths fail closed when Auth, App Check, or callable prerequisites are unavailable. Kill switch: `community_provider_enabled`. Owner: maintainer. |

## Review Notes

- Provider-returned media URLs can include CDN hosts that are not literal source
  strings. The manifest lists known direct hosts where Aura hard-codes them,
  plus provider-owned media categories in each row.
- `ccmixter-api` is the only reviewed cleartext-capable surface today because
  `network_security_config.xml` permits cleartext for `ccmixter.org`; the
  separate cleartext release gate remains open.
- Firebase hosts are SDK/project-config derived. They are included for release
  review even when they are not literal Kotlin URL strings.
